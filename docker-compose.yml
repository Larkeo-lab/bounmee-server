version: "3.8"

services:
  app:
    image: pos-server

    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - .env
    container_name: pos-server
